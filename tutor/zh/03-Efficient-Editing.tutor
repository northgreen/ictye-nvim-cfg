# 03 — 高效编辑 *efficient-editing*

本文介绍此配置中的核心编辑增强功能，帮助你大幅提升日常编辑
效率。

---

## Flash 跳转 *flash-jump*

传统 `f`/`t` 跳转需要先输入字符、再用 `;`/`,` 反复定位，效率
较低。Flash 插件将其升级为实时标记式跳转：

- `s` — 普通/可视/操作符 — 输入目标字符，自动生成标签，快速
  跳转
- `S` — 普通/可视/操作符 — Treesitter 跳转，跳到函数名、类名
  等语法节点
- `r` — 操作符 — 远程 Flash，跨窗口跳转定位
- `R` — 操作符/可视 — Treesitter 范围搜索
- `<C-s>` — 命令行 — 在命令行模式中切换 Flash 搜索

使用时按 `s` 后输入一个字符，Flash 会在所有该字符位置生成
标签（通常为两个字母），按下对应标签即可跳转。相比 `f`/`t`
的逐字移动，Flash 一步到位。

TIP：Flash 也支持操作符待决模式，例如 `ys`（来自
vim-surround）配合 Flash 可以实现可视化范围标记。

---

## 多光标编辑 *multicursor-editing*

需要同时在多个位置编辑时，多光标是最高效的工具：

- `<up>` — 普通/可视 — 在上方行添加光标
- `<down>` — 普通/可视 — 在下方行添加光标
- `<leader><up>` — 普通/可视 — 跳过上方候选
- `<leader><down>` — 普通/可视 — 跳过后方候选
- `<leader>n` — 普通 — 选择下一个匹配并添加光标
- `<leader>s` — 普通 — 跳过当前匹配
- `<leader>N` — 普通 — 选择上一个匹配并添加光标
- `<C-q>` — 普通/可视 — 手动切换光标

典型场景：将文件中所有 `foo` 改为 `bar`。将光标放在第一个
`foo` 上，按 `<leader>n` 逐个选中所有 `foo`，然后执行
`ciwbar<Esc>` 一次性修改所有实例。

---

## 代码折叠（UFO）*code-folding*

UFO 插件提供更智能的代码折叠，支持 LSP 范围和 Treesitter 语法
折叠：

- `zR` — 展开所有折叠
- `zM` — 关闭所有折叠
- `zr` — 逐级展开折叠
- `zm` — 逐级关闭折叠
- `K` — 在折叠行上预览内容；无折叠时触发 LSP hover

按 `zc` 关闭当前折叠，`zo` 打开当前折叠——这是 Neovim 内置
操作，同样生效。`K` 的双重功能尤其实用：在折叠行按 `K` 查看
折叠内容预览，在普通代码行按 `K` 触发 LSP 类型/文档悬浮窗。

---

## 查找替换 *search-replace*

- `<leader>fs` — 普通/可视 — 打开 rip-substitute 界面

启动后输入搜索模式（支持正则），然后输入替换文本。界面会实时
显示所有匹配项和替换预览，确认后才实际执行修改。比 `:%s/`
更安全地进行项目级批量替换。

---

## 窗口操作 *window-operations*

基于 Hydra 的窗口管理让你在持续模式下快速操作，无需反复按
前缀键：

- `<C-w>c` + `h/j/k/l` — 激活导航 Hydra — `h`/`l` 左右切换、
  `j`/`k` 上下切换，按其他键退出
- `<C-w>W` + `h/j/k/l` — 激活调整大小 Hydra — `h`/`l` 调整
  宽度、`j`/`k` 调整高度

启动 Hydra 后，你可以连续按方向键多次操作，直到按其他键退出。
这比传统 `Ctrl-w + 方向键` 每次都要按前缀键顺畅得多。

---

## 快速插入空行 *quick-newline*

普通模式下直接换行，无需先退出插入模式再按 `o`：

- `<CR>` — 在当前行下方插入空行（保持光标在原位）
- `<S-CR>` — 在当前行上方插入空行

当你处于插入模式但想在代码块中快速腾出空间时，可以直接跳到
想要插入的位置、回到普通模式，然后按 `<CR>` 或 `<S-CR>`。

---

## 数字增减 *number-incdec*

- `+` — 递增光标下的数字
- `-` — 递减光标下的数字

将光标放在数字上（如 `count = 5` 中的 `5`），按 `+` 变为 `6`，
按 `-` 变回 `5`。支持光标在数字字符上而非必须在数字开头。

---

## 撤销树 *undo-tree*

Neovim 内置的撤销历史是线性结构，而 undotree 将其显示为
分支树：

- `<C-x>u` — 打开/关闭 undotree
- `<leader>u` — 同上

打开的 undotree 窗口会显示所有的撤销分支。你可以切回到任意
历史状态，即使这个状态已经被后续编辑覆盖。这在误撤销后又做了
更多修改的情况下尤其有用。

---

## 综合场景：重构函数参数 *refactoring-scenario*

假设你有这样一段代码：

~~~python
def process(a, b, c):
    print(a)
    print(b)
    print(c)
~~~

你想将所有参数名改为 `x`、`y`、`z`。可以这样操作：

1. 按 `S` 然后输入 `parameter` 跳转到函数定义区（Treesitter）
2. 按 `zM` 折叠所有代码，按 `zr` 展开到合适的层级
3. 在 `a` 上按 `<leader>n` 三次，依次选中三个参数
4. 执行 `cw` `x` 修改所有光标处的参数
5. 对 `b`、`c` 重复上述操作
6. 如果改错了，按 `<leader>u` 打开 undotree 回退

---

## 练习 *exercises*

尝试以下练习来巩固所学内容。

### 练习 1: Flash 跳转

请看下面这行文本：

~~~
The quick brown fox jumps over the lazy dog.
~~~

按 `s` 然后按字母 `o` 触发 Flash 标签，再输入 `fox` 中 `o`
字母上的标签跳转过去。

✗ 按 `s` 然后按 `o`，再输入 `fox` 上的标签

### 练习 2: 多光标

将光标移到下方的 `foo` 上，按 `<leader>n` 三次选中所有三个
匹配，然后按 `ciwbar<Esc>` 将它们全部改为 `bar`：

~~~
foo bar foo baz foo qux
~~~

✗ `<leader>n` 三次，然后 `ciwbar<Esc>`

### 练习 3: 代码折叠

将光标移到下方函数上，按 `zM` 关闭所有折叠，再按 `zR` 展开
所有折叠：

~~~python
def hello():
    print("hello")
    print("world")
~~~

✗ `zM` 关闭折叠，然后 `zR` 展开所有折叠

---

## 小结 *summary*

本文覆盖了此配置中最常用的编辑增强功能。掌握它们后，你的编辑
速度会有明显提升。

接下来建议学习：
- **教程 04**：Git 工作流 — 在编辑器内完成所有 Git 操作
- **教程 05**：LSP 与代码智能 — 补全、诊断、重构
