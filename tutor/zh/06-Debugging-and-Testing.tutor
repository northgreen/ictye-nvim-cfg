# 06 调试与测试 *06-debug-test*

## 1. 调试基础 (DAP) *dap-basics*

DAP (Debug Adapter Protocol) 是调试的标准协议。
此配置通过 nvim-dap 支持多语言调试：Python、
JavaScript/TypeScript、Go、C/C++、Rust、Lua 等。
每种语言需要安装对应的调试适配器。

## 2. 设置断点 *breakpoints*

`<C-b>` 在当前行切换断点。设置后行号旁会出现
断点标记。再次按 `<C-b>` 移除断点。

## 3. 调试控制 *debug-controls*

- `<F9>` — 继续执行
- `<F10>` — 单步跳过
- `<F11>` — 单步进入
- `<F12>` — 单步跳出

启动调试后，这组按键控制执行流程。`<F10>` 最常用，
逐行执行而不进入函数内部。

## 4. DAP UI *dap-ui*

`<F6>` 切换调试 UI 面板。面板包含：

- **变量**：查看当前作用域的变量值
- **堆栈**：调用堆栈，点击跳转到对应帧
- **监视**：自定义表达式监视
- **断点**：所有断点列表

## 5. DAP Hydra 菜单 *dap-hydra*

`<leader>dp` 打开 DAP Hydra 快捷菜单：

- `c` — 继续执行
- `i` — 单步进入
- `o` — 单步跳出
- `p` — 单步跳过
- `s` — 停止调试
- `r` — 重启调试
- `d` — 断开连接
- `t` — 切换断点
- `u` — 切换 UI
- `e` — 求值表达式

Hydra 锁定按键模式，无需反复按 `<leader>dp`。

## 6. 求值表达式 *evaluate*

调试暂停时，在 DAP Hydra 中按 `e`，输入表达式
（如变量名或 `1+1`），即可查看当前状态下的
计算结果。

## 7. Lua 调试 *lua-debug*

`:IcDAP` 启动 OSV Lua 调试器。这对调试此配置
本身或 Lua 项目特别有用。启动后会附加到当前
Neovim 实例，你可以在 Lua 文件中设断点并调试。

## 8. 测试集成 (neotest) *test-integration*

`<C-x>t` 打开测试菜单：

- Run Test — 运行光标处的测试
- Watch Test — 监视模式，文件变更自动重跑
- Show Cur Output — 查看当前测试输出
- Toggle Summary — 切换测试结果概览
- Toggle Output Panel — 切换输出面板
- Run With Debug — 带 DAP 调试器运行测试

支持的语言：Python (pytest)、Go、Rust、
JavaScript/TypeScript (vitest)、Lua 等。

## 9. 测试菜单操作 *test-menu*

测试结果用颜色标识：绿色通过、红色失败、黄色跳过。
在失败结果上回车查看详细输出。Watch 模式适合 TDD
流程——修改代码后测试自动重跑。

## 10. Overseer 任务系统 *overseer*

- `<C-x>oo` — 切换任务面板
- `<C-x>or` — 运行预定义任务

Overseer 管理构建、测试、lint 等后台任务。任务面板
显示运行状态和输出日志，可随时取消或重启任务。

## 11. 综合工作流 *workflow*

一个完整的调试场景：

1. **按 `<C-b>`** 在目标行设断点
2. **启动程序**（运行测试或启动进程）
3. 执行停在断点处，**按 `<F10>`** 逐行执行
4. **查看变量**（DAP UI 或按 `e` 手动求值）
5. 发现异常值后按 `<leader>dp` + `c`
   **继续执行**
6. **按 `<F6>`** 关闭 UI，按 `s` **停止调试**
7. 修改代码，用 **`:IcTestMenu`** 重新运行测试
   验证

## 12. 小结 *summary*

你现在掌握了调试与测试的完整工具链：设断点 →
控制执行流 → 查看变量和堆栈 → 求值表达式 →
运行/调试测试。结合 Overseer 任务系统，你可以
在一个工作流中完成"编写→测试→调试→构建"的
全流程。
