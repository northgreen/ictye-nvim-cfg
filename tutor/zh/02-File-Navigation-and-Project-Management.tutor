# 02 文件导航与项目管理：高效浏览你的项目 *文件导航与项目管理*

熟练的文件导航是提升编辑效率的第一步。本教程从目录浏览、
模糊搜索到缓冲区管理，覆盖项目中找文件的完整路径。

## 1. 文件树（Neotree）*文件树*

按 `<F4>` 切换 Neotree 文件树面板。这是项目的目录结构视图，
适合浏览和组织文件：

- `j`/`k` 上下移动，回车打开文件或展开目录
- `?` 显示 Neotree 帮助——里面有很多快捷键
- 再次按 `<F4>` 关闭面板

提示：文件树适合"我大概记得文件在哪个目录"的场景。如果你
知道文件名但不记得位置，用 Telescope（下一节）更快。

## 2. 模糊查找（Telescope）*模糊查找*

Telescope 是本配置的万能搜索工具：

- `<C-f>`：搜索文件（按文件名实时过滤）
- `<C-p>`：打开 Telescope 主菜单——选择搜索模式
- `<C-x><C-b>` 或 `<C-c>`：显示缓冲区列表——在已打开的
  文件间切换

Telescope 会在窗口底部打开一个搜索框，输入即搜。支持模糊
匹配——输入文件名的几个字母就能找到。

注意：`<C-f>` 最常用，找项目文件首选。`<C-x><C-b>` 适合在
你已经打开的一堆文件间切换。

## 3. mini.files 文件管理器 *迷你文件管理器*

按 `<C-x><C-f>` 打开 mini.files。与 Telescope 的"搜索-选择"
模式不同，mini.files 让你在目录树中浏览：

- 按 `h`/`j`/`k`/`l` 移动焦点
- 按回车打开文件或进入目录
- 按 `-` 回到上级目录
- 支持文件操作：复制、移动、重命名、删除

**什么时候用它？** 当你需要浏览目录结构、管理文件（复制/
移动/重命名）或者不确定文件名的具体拼写时。

提示：`<C-x>f`（Pick 模式）适合找确切文件名，`<C-x><C-f>`
（mini.files）适合浏览目录。两者互补，用对工具事半功倍。

## 4. Oil 文件管理器 *Oil文件管理器*

按 `<leader>O`（空格然后大写 O）打开 Oil。和 mini.files 不同，
Oil 将目录以普通文本缓冲区的形式呈现——你可以像编辑文本
一样编辑文件列表：

- 删除文件行 → 删除文件
- 重命名文件行 → 重命名文件
- 新建一行 → 创建新文件

这是一个非常独特的文件管理方式，适合批量操作时使用。

## 5. 缓冲区管理 *缓冲区管理*

Neovim 的"缓冲区"（buffer）就是打开的文件。本配置提供了
丰富的缓冲区管理快捷键：

- `<S-h>` / `<S-l>`：切换到上一个/下一个缓冲区
- `[b` / `]b`：循环上一个/下一个缓冲区
- `[B` / `]B`：将当前缓冲区左移/右移一个位置
- `<leader>1`-`<leader>9`：跳转到第 1-9 号缓冲区
- `<leader>bp`：固定/取消固定当前缓冲区
- `<leader>bP`：关闭所有非固定的缓冲区
- `<leader>br`：关闭当前右侧的所有缓冲区
- `<leader>bl`：关闭当前左侧的所有缓冲区

**推荐工作流：** 用 `<S-h>`/`<S-l>` 左右切换文件；固定重要
文件（`<leader>bp`），需要清理时一键关闭所有非固定缓冲区
（`<leader>bP`）。

## 6. Bufferline 增强 *Bufferline增强*

Bufferline 在窗口顶部显示所有打开缓冲区的标签条，并提供了
额外的导航方式：

- `<leader>-` / `<leader>=`：循环上一个/下一个缓冲区
- `<C-,>` / `<C-.>`：将缓冲区左移/右移一个位置

标签条上还能看到文件修改状态（`[+]` 表示有未保存的更改）、
Git 状态指示器。

## 7. 窗口操作 *窗口操作*

当你有多个分屏时，需要快速在窗口间移动和调整大小：

**窗口导航：** 按 `<C-w>c` 然后按 `h`/`j`/`k`/`l` 移动到
左/下/上/右窗口。这是 Hydra 模式——按一次 `<C-w>c` 后可以
连续按方向键，无需重复前缀。

**窗口大小调整：** 按 `<C-w>W` 然后按 `h`/`j`/`k`/`l` 调整
窗口尺寸（`h` 缩小宽度，`l` 增加宽度，`j` 增加高度，
`k` 缩小高度）。

注意：Hydra 的意思是"多头蛇"——进入模式后你可以连续操作，
直到按 Escape 退出，非常适合窗口频繁切换的场景。

## 8. 浮动终端 *浮动终端*

在 Neovim 内部运行 shell 命令，无需切换到外部终端：

- `<C-x>ft`：打开/关闭浮动终端
- `<leader>lj`：打开 jjui（Jujutsu 版本控制界面）

浮动终端在当前窗口中央弹出，运行完命令后按 `jj` 退出插入
模式，然后按 `<C-x>ft` 关闭。

提示：本配置使用 Jujutsu (`jj`) 作为版本控制系统。按
`<leader>lj` 打开 jjui 图形界面，比命令行更直观。

## 9. Outline 符号大纲 *符号大纲*

按 `<F3>` 切换符号大纲面板。它会解析当前文件中的函数、类、
变量等符号，按层级展示。

- 用 `j`/`k` 选择符号
- 回车跳到符号定义位置
- 再次按 `<F3>` 关闭

适合快速浏览大文件的结构，或者在长文件中定位特定函数。

## 10. 会话管理 *会话管理*

会话（Session）保存和恢复你的工作区状态——包括打开的文件、
窗口布局、光标位置等：

- `<leader>ss`：保存当前会话（命名后保存）
- `<leader>sl`：加载已保存的会话
- `<leader>sd`：删除不需要的会话

**推荐工作流：** 每天下班前按 `<leader>ss` 保存会话，第二天
按 `<leader>sl` 快速恢复到昨天的状态——打开的每个文件都在
原来的位置等着你。

## 11. 小结：日常最佳实践路径 *小结*

下面是打开项目后最自然的操作流程：

~~~
1. 打开项目       nvim .
2. 浏览目录       <F4> Neotree 或者 <C-x><C-f> mini.files
3. 搜索文件       <C-f> Telescope 输入文件名
4. 切换文件       <S-h> / <S-l> 左右切换
5. 调整布局       <C-w>c + h/j/k/l 导航窗口
6. 运行命令       <C-x>ft 浮动终端
7. 查看结构       <F3> Outline
8. 保存状态       <leader>ss 保存会话
~~~

掌握了这些，你已经能够高效地在项目中浏览和操作文件。接下来
推荐学习 **教程 03：高效编辑**，学习 Flash 跳转、多光标编辑
和文本操作技巧。
