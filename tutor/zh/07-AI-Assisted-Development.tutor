# 07 — AI 编程辅助 *ai-tutorial*

本文介绍此配置中的 AI 集成工具：OpenCode 内联 AI 对话、
Avante 多提供商支持、Fitten Code 自动补全，以及 MCP 文
件感知集成。

---

## OpenCode 简介 *opencode-overview*

OpenCode 是此配置中的核心 AI 编程助手。它运行在 Neovim
内部，无需切换窗口或复制粘贴代码。你可以通过聊天气泡和
操作符驱动的代码操作与 AI 交互。

- **用途** — 与 AI 对话，发送代码，获取建议
- **界面** — 内联聊天气泡 + 侧边面板
- **提供商** — 可配置（默认使用当前 AI 后端）

---

## 基本用法：向 AI 提问 *basic-usage*

普通模式下按 `<C-a>` 打开 OpenCode 聊天气泡。当前文件会
自动作为上下文发送。输入问题或指令后回车，AI 会在编辑器
中内联回复代码建议、解释或重构方案。

- `<C-a>`（普通模式） — 打开 AI 聊天气泡（当前文件作为
  上下文）

**示例**：打开一个 Python 文件，按 `<C-a>`，输入"为文件
操作添加错误处理"——AI 会分析当前文件并生成建议代码。

---

## 选择模式：局部代码提问 *selection-mode*

选中代码后按 `<C-a>`——选中内容自动成为 AI 的上下文。当
你只想让 AI 关注某个特定函数或代码块时非常有用。

- `<C-a>`（可视模式） — 用选中代码作为上下文提问

**示例**：选中一个慢循环，按 `<C-a>`，输入"优化这段代码"
——AI 只看到选中的代码并提出改进建议。

---

## Operator 模式：`go` 和 `goo` *operator-mode*

`go` 是一个操作符，通过 motion 定义的范围将代码发送给
OpenCode。可以把它想象成 `y`（复制）但是给 AI 用的——
你用动作来选择发送什么。

- `go`（普通/可视模式） — 将 motion 范围发送到 OpenCode
- `goo`（普通模式） — 发送当前行（行级操作符）

**实用组合**：

- `goiw` — 当前光标处的单词
- `goap` — 当前段落
- `goi(` — 括号内的内容
- `goG` — 光标到文件末尾的全部内容
- `go%` — 当前花括号块的代码

发送后 AI 聊天气泡会自动打开，你可以接着输入问题。

---

## 面板操作 *panel-operations*

OpenCode 提供侧边面板用于查看对话历史。

- `<leader>oC`（普通/终端模式） — 切换 OpenCode 面板
- `<S-C-u>`（普通模式） — 向上滚动面板内容
- `<S-C-d>`（普通模式） — 向下滚动面板内容

使用面板回顾之前的 AI 对话、重新查看被你关掉的代码建议，
或在长回复中上下浏览。

---

## Avante 多提供商支持 *avante*

此配置同时集成了 **Avante.nvim**，支持多个 AI 提供商切
换。

- **OpenAI (GPT-4o)** — 通用编程辅助
- **GitHub Models** — 通过 GitHub Copilot 免费使用
- **SiliconFlow** — 中文友好的备选提供商

使用 Avante 的命令：

- `:AvanteAsk` — 在 Avante 中提问
- `:AvanteEdit` — 用 AI 编辑选中代码
- `:AvanteToggle` — 切换 Avante 面板

**何时用 Avante vs OpenCode**：OpenCode 更轻量，适合快
速内联提问。Avante 更适合需要持续对话或切换提供商的场景。

---

## Fitten Code 补全 *fitten-code*

**Fitten Code** 提供 AI 驱动代码自动补全，在你输入时自
动激活。与 OpenCode（需要手动召唤）不同，Fitten Code 在
后台静默工作：

- **内联补全** — 输入时显示虚拟文本建议
- **自动激活** — 无需按键——补全自动出现
- **接受建议** — 按 `<Tab>` 接受补全内容

安装后无需额外配置即可使用。

---

## MCP 集成 *mcp*

Model Context Protocol (MCP) 让 AI 能够访问外部工具和文
件系统。这使得 AI 可以读取文件、搜索内容，以可控方式与
项目交互。

- **文件读取** — AI 可以读取你引用的特定文件
- **文件搜索** — AI 可以对项目文件进行 grep 搜索
- **工具执行** — AI 可以调用已授权的外部工具

MCP 采用集中配置，通过 OpenCode 和 Avante 自动生效。

---

## 使用建议 *usage-tips*

**选择合适的工具**：

- **OpenCode (`<C-a>`)** — 快速内联提问、代码操作、
  operator 驱动上下文
- **Avante (`:AvanteAsk`)** — 多轮持续对话、切换提供商
- **Fitten Code** — 打字时的被动补全
- **MCP** — AI 需要读取/搜索文件时的深度项目理解

**编写有效提示的技巧**：

- 具体明确："将此函数改为提前返回模式"而非"优化一下"
- 指定输出格式："返回 diff 格式的代码"或"用要点列出原因"
- 用 operator 模式（`go`）精确限定 AI 的上下文——更少
  的噪音 = 更好的答案
- 对特定代码块用 `<C-a>` 选择模式提问，精准定位

---

## 小结 *summary*

你现在在编辑器中有四种 AI 工具可用：

1. **OpenCode**——内联对话、operator 驱动，`<C-a>` 提问、
   `go` 发送上下文
2. **Avante**——多提供商对话，适合持续会话场景
3. **Fitten Code**——后台静默自动补全
4. **MCP**——文件感知的 AI 工具集成

建议先从 `<C-a>` 开始快速提问，需要长对话时再使用
Avante。Operator 模式（`go`/`goo`）是向 AI 喂代码最精确
的方式——多练习与文本对象的组合使用。
