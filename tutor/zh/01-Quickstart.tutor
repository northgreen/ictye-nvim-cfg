# 01 快速上手：从安装到第一次编辑 *快速上手*

欢迎使用 ictye-nvim-cfg！这篇教程带你完成首次启动、了解基本
界面，并掌握最常用的操作。读完本文大约需要 10 分钟。

## 1. 启动配置 *启动配置*

安装完成后，直接在终端输入 `nvim` 即可启动全功能模式——
所有插件、LSP、AI 工具全部加载。

如果你只需要快速编辑一个文件，不想等待全部插件加载，
可以用 Lite 模式：

~~~bash
nvim --cmd "let g:lite_mode=v:true"
~~~

Lite 模式只加载最少的插件（基础编辑、语法高亮、文件树），
启动速度明显更快。两种模式共享同一套配置文件，切换无负担。

## 2. 了解你的界面 *界面介绍*

启动后你会看到几个关键区域：

- **底部状态栏**（lualine）—— 显示当前文件名、Git 分支、
  光标位置、LSP 状态
- **顶部标签栏**（bufferline）—— 列出所有打开的缓冲区
  （文件标签），高亮当前文件
- **右侧/左侧边栏**（edgy）—— 根据上下文自动显示 outline、
  终端、debug 面板

注意：初次启动时界面可能看起来比较简洁，别担心——打开几个
文件后标签栏和状态信息就会丰富起来。

## 3. 第一个要记住的操作：退出插入模式 *退出插入模式*

按 `jj`（连续按两次 j）退出插入模式返回普通模式。这是本配置
最重要的习惯——比按 Escape 快得多，手不用移动。

~~~
按 i 进入插入模式 → 输入文字 → 按 jj 回到普通模式
~~~

## 4. 打开文件 *打开文件*

按 `<C-x>f`（Ctrl+x 然后 f）打开 Telescope 文件搜索。开始
输入文件名的一部分，搜索结果会实时过滤——这是最常用的文件
打开方式。

如果你更习惯浏览目录结构，按 `<C-x><C-f>` 打开 mini.files，
一个基于目录树的文件管理器。用 `h`/`j`/`k`/`l` 导航，
按回车打开文件。

小提示：`<C-x>` 是本配置的"命令前缀"，很多功能都以这个
组合开始。

## 5. QuickMenu 快捷菜单 *快捷菜单*

按 `<F5>` 或 `<C-x>q` 打开 QuickMenu。这是一个多功能菜单，
包含：

- **Search** — 搜索文件、文本、符号
- **Git** — Git 操作入口
- **Debug** — 调试功能
- **Tasks** — 运行任务
- **Run** — 快速运行命令

方向键或 `j`/`k` 选择，回车确认。试试看——它基本涵盖了
日常高频操作。

## 6. 关闭当前文件 *关闭当前文件*

按 `<leader>q`（空格键然后 q）关闭当前缓冲区。这个命令会
切到上一个缓冲区然后关闭当前文件，不会意外退出 Neovim。

## 7. 预览图片 *预览图片*

当光标停在图片文件路径上时，按 `gf` 会在悬浮窗口中显示
图片预览（Snacks image hover）。支持常见图片格式，查看
项目内的资源文件很方便。

## 8. 探索快捷键 *探索快捷键*

- 按 `<leader>?`（空格然后问号）打开 **which-key** 弹出
  窗口，展示当前模式下的所有可用快捷键
- 输入 `:help` 查阅 Neovim 内置文档
- 输入 `:help ictye-nvim-cfg` 查看本配置的特定帮助

重要提示：which-key 是你最好的学习工具——想不起来快捷键时
按 `<leader>?` 看看。

## 9. 常用诊断命令 *诊断命令*

上手阶段记住这几个命令就够了：

- `:Lazy`：打开插件管理器——查看插件状态、更新、检查加载
  时间
- `:Mason`：安装 LSP 服务器、格式化工具、DAP 调试器
- `:checkhealth`：全面诊断 Neovim 环境和配置健康状况
- `:LspInfo`：查看当前文件的 LSP 服务器状态

安装新语言的 LSP 支持：`:MasonInstall <server-name>`
（例如 `:MasonInstall rust-analyzer`）。

## 10. 编辑配置 *编辑配置*

输入 `:ConfigEdit nvim` 快速打开本配置的核心文件。需要调整
设置或添加插件时，这是最快的入口。

## 11. 互动练习 *互动练习*

通过以下练习巩固你学到的知识。请按说明操作。

### 练习 1：使用 jj 退出插入模式 *练习-jj*

1. 按 `i` 进入插入模式。
2. 在下面这行中输入一些文字（例如"你好"）。
3. 按 `jj` 退出插入模式返回普通模式。

✗ 按 i，在这行输入文字，然后按 jj

### 练习 2：用 Telescope 打开文件 *练习-telescope*

1. 按 `<C-x>f` 打开 Telescope 文件搜索。
2. 输入文件名的一部分来过滤结果。
3. 按回车选择一个文件打开。
4. 按 `<leader>q` 关闭缓冲区。

✗ 按 <C-x>f，搜索文件，打开它，再关闭它

### 练习 3：切换缓冲区 *练习-缓冲区*

1. 如果打开了多个文件，按 `<S-l>`（Shift+l）切换到下一个
   缓冲区。
2. 按 `<S-h>`（Shift+h）切换到上一个缓冲区。

✗ 打开两个文件，然后用 <S-h> 和 <S-l> 来回切换

## 12. 小结 *小结*

你已经学会了：

- ✅ 全功能和 Lite 两种启动模式
- ✅ `jj` 退出插入模式
- ✅ `<C-x>f` 打开文件和 `<C-x><C-f>` 浏览目录
- ✅ QuickMenu 和 which-key 探索工具
- ✅ 关闭缓冲区、预览图片
- ✅ 常用诊断命令和配置编辑入口

下一步推荐学习 **教程 02：文件导航与项目管理**，深入了解
文件树、模糊搜索、缓冲区管理和会话管理。
